#include <stdio.h>

int main(int argc, char** argv) {
    if(argv[1][0] > 65) {
        printf("EqualProgress");
    } else {
        printf("EqualProgress");
    }
    while(argv[1][0] == 'a'){}
    return 0;
}