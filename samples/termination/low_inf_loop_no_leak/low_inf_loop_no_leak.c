#include <stdio.h>

int main(int argc, char** argv) {
    if(argc == 2) {
        printf("EqualProgress");
    } else {
        printf("EqualProgress");
    }
    while(argv[1][0] == 'a'){}
    return 0;
}